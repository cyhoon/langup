import React from 'react';
import LoginContainer from '../containers/LoginContainer';

const Test = () => (
    <div style={{ height: '100%' }}>
        <LoginContainer />
    </div>
);

export default Test;